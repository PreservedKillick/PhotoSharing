<% content_for(:title, "Photo | PhotoShare") %>

<h2><%= @user.name %>'s Photo</h2>

<h5><%= @photo.image_file_name %></h5>

<%= image_tag @photo.image.url(:medium) %>

<br>
<br>

<div class="row">
  <div class="col-lg-12 inline">
    <%= link_to "Profile Page", user_path(@user), class: "btn btn-warning" %>
  </div>
</div>
